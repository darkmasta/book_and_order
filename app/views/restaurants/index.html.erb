<div class="container">
  <div class="jumbotron">
    <h1>Restaurantlar</h1>

    <div class="row">
      <% @restaurants.each do |r| %>
        <div class="span4"><%= r.name %></div>
      <% end %>
    </div>
  </div>
</div>
